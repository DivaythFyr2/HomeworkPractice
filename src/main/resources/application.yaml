server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:keystore.p12
    key-store-type: PKCS12
    key-store-password: password123
    key-alias: homeworkpractice

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/security_db
    username: postgres
    password: qwerty229
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
      schema-locations: classpath:sql/schema.sql
      data-locations: classpath:sql/data.sql

security:
  jwt:
    secret: "MnDiyBFcgZo1UL7wsqYv5ZkhwxKssmiHUZtIOiQxl1o="
    access-expiration-ms: 900000      # 15
    refresh-expiration-ms: 604800000  # 7