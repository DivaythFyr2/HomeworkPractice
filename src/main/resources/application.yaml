spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/library_db
    username: postgres
    password: qwerty229
    driver-class-name: org.postgresql.Driver

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data-for-table-book.sql
      encoding: UTF-8
